import * as client from '../../../client';
import { startSetup } from './start';

import {
  SLACK_BOT_ID,
  SLACK_BOT_TOKEN,
  SLACK_PRIVATE_CHANNEL,
  SLACK_USER_ID,
  SLACK_USER_TOKEN,
} from '../../../../test/config';

describe('Interactions - Setup', () => {

  it('Should Pass', async () => {

    expect(true).toBe(true);

  });
});
